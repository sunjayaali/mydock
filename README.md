# Development environment based on Docker

## Getting Started

### Prerequisites

- Ensure [Docker](https://www.docker.com/) and [Docker Compose](https://docs.docker.com/) are installed.
- Copy the example environment file:

      cp .env.example .env

### Run the environment

To start all services, run:

    docker compose up -d
  